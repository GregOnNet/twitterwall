export class AppConfig {
  static apiPath = 'http://localhost:3000/api/v1/';
  static search = '#nossued';
  static handle = '@NOSSued';
  static legalPage = 'http://nossued.de/#impressum';
  static maxAge = 2; //days
  static maxCount = 50;
  static noRetweeted = true; //do not show retweeted statuses
  static scrollIntervall = 40 * 1000;  // 40 seconds
}
